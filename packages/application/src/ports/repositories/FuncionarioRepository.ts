import type { Funcionario } from '@sudo-sys/domain'; export interface FuncionarioRepository { findByEmpresa(empresaId: string): Promise<Funcionario[]>; findById(id: string): Promise<Funcionario | null>; save(f: Omit<Funcionario,'id'>): Promise<Funcionario>; update(id: string, f: Partial<Funcionario>): Promise<Funcionario>; delete(id: string): Promise<void> }
