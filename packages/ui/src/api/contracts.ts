// Reexporta os tipos do pacote shared para uso no renderer
export type {
  IpcResponse,
  DbType,
  DbConfig,
  EmpresaConfig,
  AppConfig,
  TestDbPayload,
  TestDbResult,
  SaveConfigPayload,
} from '@sudo-sys/shared'
