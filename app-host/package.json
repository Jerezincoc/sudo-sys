{
  "name": "app-host",
  "private": true,
  "version": "0.0.1",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm -w -r --filter @sudo/infrastructure --filter app-host build && electron ."
  },
  "dependencies": {
    "@sudo/infrastructure": "workspace:*",
    "sqlite3": "^5.1.7"
  }
}